# Single source of truth for the BizBill version.
# versionCode MUST increase for every release (CI enforces this).
# NOTE: set these to your currently published values before the first
# release if they are higher; Android refuses to install a lower versionCode.
VERSION_NAME=3.5
VERSION_CODE=13
PACKAGE_NAME=tech.bbmfg.bizbill
